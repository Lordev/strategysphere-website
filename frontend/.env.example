# Example env for frontend (copy to frontend/.env and provide real values)
# Do NOT commit real secrets. Add frontend/.env to .gitignore.

# -----------------------------
# Sanity configuration
# -----------------------------
# Public project identifier for your Sanity project. This is safe to expose client-side.
NEXT_PUBLIC_SANITY_PROJECT_ID=tpz25z77

# Dataset name (usually `production` or `staging`). Used by the Sanity client.
NEXT_PUBLIC_SANITY_DATASET=production

# Optional: server-side read token. Only use this for server-to-Sanity requests.
# Do NOT expose this token in client bundles or commit it to the repo.
# SANITY_READ_TOKEN=your_read_token_here

# -----------------------------
# Frontend behavior / dev helpers
# -----------------------------
# Number of posts fetched per “Load more” request on the blog page (default: 6).
# Parsed as a number in the app: Number(process.env.NEXT_PUBLIC_POST_AMOUNT) || 6
NEXT_PUBLIC_POST_AMOUNT=6

